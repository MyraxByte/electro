/**
 * Represents a metadata key accepted by the Reflect Metadata API.
 */
export type MetadataKey = string | symbol;

/**
 * Represents a valid JavaScript property key used by decorators and metadata.
 */
export type PropertyKey = string | symbol;
