/**
 * Metadata key for `@Module()`.
 */
export const MODULE_METADATA = Symbol.for("electro:module");

/**
 * Metadata key for `@Injectable()`.
 */
export const INJECTABLE_METADATA = Symbol.for("electro:injectable");

/**
 * Metadata key for `@Window()`.
 */
export const WINDOW_METADATA = Symbol.for("electro:window");

/**
 * Metadata key for `@View()`.
 */
export const VIEW_METADATA = Symbol.for("electro:view");

/**
 * Metadata key for `@command()`.
 */
export const COMMAND_METADATA = Symbol.for("electro:command");

/**
 * Metadata key for `@query()`.
 */
export const QUERY_METADATA = Symbol.for("electro:query");

/**
 * Metadata key for `@job()`.
 */
export const JOB_METADATA = Symbol.for("electro:job");

/**
 * Metadata key for `@signal()`.
 */
export const SIGNAL_METADATA = Symbol.for("electro:signal");
